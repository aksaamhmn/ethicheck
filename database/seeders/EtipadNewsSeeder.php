<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\EtipadNews;
use Illuminate\Support\Str;

class EtipadNewsSeeder extends Seeder
{
    public function run(): void
    {
        // Disabled: sample news seeder is intentionally left empty per user request.
        // This class remains only to avoid autoload errors if referenced; it seeds nothing.
        return;
    }
}
